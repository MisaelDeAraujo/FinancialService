spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/financialservicedb
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
